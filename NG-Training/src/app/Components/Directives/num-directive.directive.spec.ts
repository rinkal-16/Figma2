import { NumDirectiveDirective } from './num-directive.directive';

describe('NumDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
